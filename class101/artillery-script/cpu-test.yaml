config:
  target: "http://34.64.81.243"
  phases:
    - duration: 360
      arrivalRate: 1
      name: Warm up

scenarios:
  # We define one scenario:
  - name: "just get hash"
    flow:
      - get:
          url: "/hash/123"
