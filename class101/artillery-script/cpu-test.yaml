config:
  target: "http://34.80.231.65/"
  phases:
    - duration: 180
      arrivalRate: 2
      name: Warm up

scenarios:
  # We define one scenario:
  - name: "just get hash"
    flow:
      - get:
          url: "/hash/123"
