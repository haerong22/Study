server:
  port: 9001

spring:
  application:
    name: book-api

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
    register-with-eureka: true
    fetch-registry: true