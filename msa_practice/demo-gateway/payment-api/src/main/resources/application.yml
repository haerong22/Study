server:
  port: 9002

spring:
  application:
    name: payment-api

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
    register-with-eureka: true
    fetch-registry: true